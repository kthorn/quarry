# Seed Data + Seed Command Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a YAML seed data file with ~25 AI/robotics/cutting-edge-tech companies and a `seed()` function to load them into the DB.

**Architecture:** `seed_data.yaml` at project root holds company records. `quarry/agent/tools.py` contains `seed()` which reads the YAML, creates `Company` models, and inserts via the existing `Database.insert_company()` method. A `click` CLI entry point allows `python -m quarry.agent.tools seed`.

**Tech Stack:** Python, PyYAML (already in requirements), click (already used), sqlite3 via existing `Database` class

---

### Task 1: Create `seed_data.yaml` with ~25 companies

**Files:**
- Create: `seed_data.yaml`

- [ ] **Step 1: Create the seed data YAML file**

Create `seed_data.yaml` at project root with ~25 AI/robotics/cutting-edge-tech companies. Each entry uses real ATS slugs where known. Companies are a mix of Greenhouse, Lever, Ashby, and generic careers pages.

```yaml
- name: OpenAI
  domain: openai.com
  careers_url: https://openai.com/careers
  ats_type: greenhouse
  ats_slug: openai
  crawl_priority: 8
  added_reason: Leading AI lab, GPT/Codex

- name: Anthropic
  domain: anthropic.com
  careers_url: https://www.anthropic.com/careers
  ats_type: greenhouse
  ats_slug: anthropic
  crawl_priority: 9
  added_reason: AI safety, Claude

- name: xAI
  domain: x.ai
  careers_url: https://boards.greenhouse.io/xai
  ats_type: greenhouse
  ats_slug: xai
  crawl_priority: 7
  added_reason: Grok, Elon Musk AI lab

- name: DeepMind
  domain: deepmind.google
  careers_url: https://www.deepmind.com/careers
  ats_type: generic
  ats_slug: null
  crawl_priority: 7
  added_reason: Leading AI research lab

- name: Hugging Face
  domain: huggingface.co
  careers_url: https://huggingface.co/jobs
  ats_type: lever
  ats_slug: huggingface
  crawl_priority: 8
  added_reason: Open-source AI platform, transformers

- name: Scale AI
  domain: scale.com
  careers_url: https://scale.com/careers
  ats_type: greenhouse
  ats_slug: scaleai
  crawl_priority: 7
  added_reason: Data infrastructure for AI

- name: Databricks
  domain: databricks.com
  careers_url: https://www.databricks.com/company/careers
  ats_type: greenhouse
  ats_slug: databricks
  crawl_priority: 7
  added_reason: Data + AI platform, ML infrastructure

- name: Cohere
  domain: cohere.com
  careers_url: https://boards.greenhouse.io/cohereai
  ats_type: greenhouse
  ats_slug: cohereai
  crawl_priority: 7
  added_reason: Enterprise LLM provider

- name: Mistral AI
  domain: mistral.ai
  careers_url: https://mistral.ai/careers
  ats_type: lever
  ats_slug: mistrl
  crawl_priority: 7
  added_reason: European LLM company, frontier models

- name: Figure AI
  domain: figure.ai
  careers_url: https://boards.greenhouse.io/figureai
  ats_type: greenhouse
  ats_slug: figureai
  crawl_priority: 8
  added_reason: Humanoid robotics, AI

- name: Tesla
  domain: tesla.com
  careers_url: https://www.tesla.com/careers
  ats_type: generic
  ats_slug: null
  crawl_priority: 6
  added_reason: Autonomy, robotics (Optimus), energy

- name: Boston Dynamics
  domain: bostondynamics.com
  careers_url: https://www.bostondynamics.com/careers
  ats_type: greenhouse
  ats_slug: bostondynamics
  crawl_priority: 7
  added_reason: Leading robotics company

- name: Agility Robotics
  domain: agilityrobotics.com
  careers_url: https://boards.greenhouse.io/agilityrobotics
  ats_type: greenhouse
  ats_slug: agilityrobotics
  crawl_priority: 6
  added_reason: Digit humanoid robot

- name: Symbotic
  domain: symbotic.com
  careers_url: https://www.symbotic.com/careers
  ats_type: greenhouse
  ats_slug: symbotic
  crawl_priority: 6
  added_reason: AI-driven warehouse robotics

- name: Cursor (Anysphere)
  domain: cursor.sh
  careers_url: https://boards.greenhouse.io/anysphere
  ats_type: greenhouse
  ats_slug: anysphere
  crawl_priority: 8
  added_reason: AI code editor, fast-growing

- name: Perplexity AI
  domain: perplexity.ai
  careers_url: https://boards.greenhouse.io/perplexityai
  ats_type: greenhouse
  ats_slug: perplexityai
  crawl_priority: 7
  added_reason: AI search engine

- name: Adept AI
  domain: adept.ai
  careers_url: https://boards.greenhouse.io/adeptai
  ats_type: greenhouse
  ats_slug: adeptai
  crawl_priority: 6
  added_reason: AI agents, action models

- name: Physical Intelligence
  domain: physicalintelligence.company
  careers_url: https://boards.greenhouse.io/physicalintelligence
  ats_type: greenhouse
  ats_slug: physicalintelligence
  crawl_priority: 7
  added_reason: Foundation models for robotics

- name: Nimble Robotics
  domain: nimble.ai
  careers_url: https://jobs.lever.co/NimbleAI
  ats_type: lever
  ats_slug: NimbleAI
  crawl_priority: 6
  added_reason: Autonomous logistics robotics

- name: Field AI
  domain: field-ai.com
  careers_url: https://jobs.lever.co/field-ai
  ats_type: lever
  ats_slug: field-ai
  crawl_priority: 6
  added_reason: Field-ready robotic AI

- name: Wandb
  domain: wandb.ai
  careers_url: https://boards.greenhouse.io/wandb
  ats_type: greenhouse
  ats_slug: wandb
  crawl_priority: 6
  added_reason: MLOps platform, experiment tracking

- name: Runway
  domain: runwayml.com
  careers_url: https://boards.greenhouse.io/runway
  ats_type: greenhouse
  ats_slug: runway
  crawl_priority: 6
  added_reason: AI creative tools, generative media

- name: Together AI
  domain: together.ai
  careers_url: https://boards.greenhouse.io/togetherai
  ats_type: greenhouse
  ats_slug: togetherai
  crawl_priority: 6
  added_reason: AI cloud infrastructure, open-source models

- name: Reka
  domain: reka.ai
  careers_url: https://boards.greenhouse.io/reka
  ats_type: greenhouse
  ats_slug: reka
  crawl_priority: 6
  added_reason: Multimodal AI models

- name: Vantage Robotics
  domain: vantage-robotics.com
  careers_url: https://www.vantage-robotics.com/careers
  ats_type: generic
  ats_slug: null
  crawl_priority: 5
  added_reason: Aerial robotics, drones

- name: Carbon Robotics
  domain: carbonrobotics.com
  careers_url: https://www.carbonrobotics.com/careers
  ats_type: greenhouse
  ats_slug: carbonrobotics
  crawl_priority: 5
  added_reason: AI-powered LaserWeeder, agricultural robotics

- name: Covariant
  domain: covariant.ai
  careers_url: https://boards.greenhouse.io/covariant
  ats_type: greenhouse
  ats_slug: covariant
  crawl_priority: 6
  added_reason: AI robotics for warehouse automation
```

- [ ] **Step 2: Commit seed_data.yaml**

```bash
git add seed_data.yaml
git commit -m "Add seed_data.yaml with 27 AI/robotics companies"
```

---

### Task 2: Create `quarry/agent/__init__.py` and `quarry/agent/tools.py`

**Files:**
- Create: `quarry/agent/__init__.py`
- Create: `quarry/agent/tools.py`

- [ ] **Step 1: Create the agent package `__init__.py`**

Create `quarry/agent/__init__.py`:

```python
"""Quarry agent tools and commands."""
```

- [ ] **Step 2: Create `quarry/agent/tools.py` with seed function and CLI**

Create `quarry/agent/tools.py`:

```python
import sys
from pathlib import Path

import click
import yaml

from quarry.config import settings
from quarry.models import Company
from quarry.store.db import Database, init_db


def seed(db: Database | None = None, seed_file: str | None = None) -> tuple[int, int]:
    """Load companies from seed_data.yaml into the database.

    Args:
        db: Database instance (loads from config if None)
        seed_file: Path to YAML file (uses config if None)

    Returns:
        Tuple of (inserted_count, skipped_count)
    """
    if db is None:
        db = Database(settings.db_path)

    seed_path = Path(seed_file or settings.seed_file)

    if not seed_path.exists():
        print(f"Error: seed file not found: {seed_path}", file=sys.stderr)
        sys.exit(1)

    with open(seed_path) as f:
        companies_data = yaml.safe_load(f)

    if not companies_data:
        print("Error: seed file is empty", file=sys.stderr)
        sys.exit(1)

    existing = {c.name for c in db.get_all_companies(active_only=False)}

    inserted = 0
    skipped = 0

    for entry in companies_data:
        name = entry.get("name")
        if not name:
            print(f"Warning: skipping entry with no name: {entry}", file=sys.stderr)
            skipped += 1
            continue

        if name in existing:
            skipped += 1
            continue

        company = Company(
            name=name,
            domain=entry.get("domain"),
            careers_url=entry.get("careers_url"),
            ats_type=entry.get("ats_type", "unknown"),
            ats_slug=entry.get("ats_slug"),
            active=entry.get("active", True),
            crawl_priority=entry.get("crawl_priority", 5),
            notes=entry.get("notes"),
            added_by="seed",
            added_reason=entry.get("added_reason"),
        )
        db.insert_company(company)
        existing.add(name)
        inserted += 1

    print(f"Seeded {inserted} companies, skipped {skipped}")
    return inserted, skipped


@click.group()
def cli():
    """Quarry agent tools."""
    pass


@cli.command()
@click.option("--seed-file", default=None, help="Path to seed YAML file")
def seed_cmd(seed_file):
    """Seed the database with companies from YAML file."""
    db = init_db(settings.db_path)
    seed(db=db, seed_file=seed_file)


if __name__ == "__main__":
    cli()
```

- [ ] **Step 3: Commit the agent tools module**

```bash
git add quarry/agent/__init__.py quarry/agent/tools.py
git commit -m "Add quarry.agent.tools with seed command"
```

---

### Task 3: Wire up CLI entry point

The AGENTS.md says `python -m quarry.agent.tools seed` should work. The `click` CLI uses `seed_cmd` as the command name. We need to either rename it or adjust the invocation.

**Files:**
- Modify: `quarry/agent/tools.py`

- [ ] **Step 1: Rename the click command to `seed`**

In `quarry/agent/tools.py`, rename `seed_cmd` to `seed` and apply the `@cli.command()` decorator. Since `seed` is already used as the function name for the programmatic entry point, name the click command function `seed_command` but give it the CLI name `seed`:

Update the CLI section of `quarry/agent/tools.py`:

```python
@cli.command(name="seed")
@click.option("--seed-file", default=None, help="Path to seed YAML file")
def seed_command(seed_file):
    """Seed the database with companies from YAML file."""
    db = init_db(settings.db_path)
    seed(db=db, seed_file=seed_file)
```

This makes `python -m quarry.agent.tools seed` work as documented in AGENTS.md.

- [ ] **Step 2: Test the CLI invocation**

```bash
python -m quarry.agent.tools seed
```

Expected: output showing "Seeded X companies, skipped Y"

- [ ] **Step 3: Commit**

```bash
git add quarry/agent/tools.py
git commit -m "Wire up CLI entry point for seed command"
```

---

### Task 4: Write tests for seed functionality

**Files:**
- Create: `tests/test_seed.py`

- [ ] **Step 1: Write test file**

Create `tests/test_seed.py`:

```python
import yaml
from pathlib import Path

from quarry.models import Company
from quarry.store.db import Database, init_db
from quarry.agent.tools import seed


def _write_seed_file(tmp_path: Path, companies: list[dict]) -> Path:
    seed_file = tmp_path / "seed_data.yaml"
    with open(seed_file, "w") as f:
        yaml.dump(companies, f)
    return seed_file


def test_seed_inserts_companies(tmp_path):
    db_path = tmp_path / "test.db"
    db = init_db(db_path)
    seed_file = _write_seed_file(tmp_path, [
        {"name": "TestCorp", "domain": "testcorp.com", "ats_type": "greenhouse", "ats_slug": "testcorp"},
        {"name": "AICo", "domain": "aico.com", "ats_type": "lever", "ats_slug": "aico"},
    ])

    inserted, skipped = seed(db=db, seed_file=str(seed_file))

    assert inserted == 2
    assert skipped == 0

    companies = db.get_all_companies(active_only=False)
    names = {c.name for c in companies}
    assert "TestCorp" in names
    assert "AICo" in names


def test_seed_skips_duplicates(tmp_path):
    db_path = tmp_path / "test.db"
    db = init_db(db_path)
    seed_file = _write_seed_file(tmp_path, [
        {"name": "TestCorp", "domain": "testcorp.com"},
    ])

    db.insert_company(Company(name="TestCorp", domain="testcorp.com"))

    inserted, skipped = seed(db=db, seed_file=str(seed_file))

    assert inserted == 0
    assert skipped == 1


def test_seed_mixed_insert_and_skip(tmp_path):
    db_path = tmp_path / "test.db"
    db = init_db(db_path)
    seed_file = _write_seed_file(tmp_path, [
        {"name": "ExistingCo", "domain": "existing.com"},
        {"name": "NewCo", "domain": "newco.com"},
    ])

    db.insert_company(Company(name="ExistingCo", domain="existing.com"))

    inserted, skipped = seed(db=db, seed_file=str(seed_file))

    assert inserted == 1
    assert skipped == 1

    companies = db.get_all_companies(active_only=False)
    names = {c.name for c in companies}
    assert "NewCo" in names


def test_seed_sets_added_by(tmp_path):
    db_path = tmp_path / "test.db"
    db = init_db(db_path)
    seed_file = _write_seed_file(tmp_path, [
        {"name": "TestCorp", "domain": "testcorp.com"},
    ])

    seed(db=db, seed_file=str(seed_file))

    companies = db.get_all_companies(active_only=False)
    assert companies[0].added_by == "seed"


def test_seed_preserves_ats_fields(tmp_path):
    db_path = tmp_path / "test.db"
    db = init_db(db_path)
    seed_file = _write_seed_file(tmp_path, [
        {
            "name": "TestCorp",
            "domain": "testcorp.com",
            "ats_type": "greenhouse",
            "ats_slug": "testcorp",
            "crawl_priority": 8,
            "added_reason": "Leading AI lab",
        },
    ])

    seed(db=db, seed_file=str(seed_file))

    companies = db.get_all_companies(active_only=False)
    c = companies[0]
    assert c.ats_type == "greenhouse"
    assert c.ats_slug == "testcorp"
    assert c.crawl_priority == 8
    assert c.added_reason == "Leading AI lab"
```

- [ ] **Step 2: Run the tests**

```bash
python -m pytest tests/test_seed.py -v
```

Expected: All 5 tests pass.

- [ ] **Step 3: Commit**

```bash
git add tests/test_seed.py
git commit -m "Add tests for seed functionality"
```

---

### Task 5: Run full test suite and lint

- [ ] **Step 1: Run all tests**

```bash
PYTHONPATH=/home/kurtt/job-search python -m pytest tests/ -v
```

Expected: All tests pass (existing + new seed tests).

- [ ] **Step 2: Run linter**

```bash
cd /home/kurtt/job-search/quarry && ruff check .
```

Expected: No errors.

- [ ] **Step 3: Run type checker**

```bash
PYTHONPATH=/home/kurtt/job-search pyright quarry/
```

Expected: No errors (or only pre-existing ones).

---

Self-review checklist:

1. **Spec coverage**: Design doc covers seed_data.yaml (~25 companies), seed() function in quarry/agent/tools.py, CLI entry point, and error handling. All are covered by tasks 1-3.

2. **Placeholder scan**: No TBDs, TODOs, or vague steps. All code is complete.

3. **Type consistency**: `seed()` returns `tuple[int, int]`, Company model fields match the schema, Database.insert_company() takes a Company model - all consistent.